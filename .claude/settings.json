{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit *)",
            "shell": "bash",
            "timeout": 120,
            "statusMessage": "Prüfe TypeScript-Build vor dem Commit…",
            "command": "cd \"${CLAUDE_PROJECT_DIR:-.}\" 2>/dev/null || exit 0; [ -d node_modules ] || exit 0; if ! out=$(npm run lint 2>&1); then { echo \"❌ TypeScript-Build-Fehler — der Commit wurde blockiert, damit kein kaputter Build auf Vercel landet. Bitte diese Fehler beheben:\"; echo \"$out\" | tail -30; } >&2; exit 2; fi"
          }
        ]
      }
    ]
  }
}
